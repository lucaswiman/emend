# Deferred / Future

## Goal

Track ideas that are plausible but should not block the core workflow and
hot-buffer improvements.

## TODO

- [ ] Explore sink-aware helper ranking near known sink contexts
- [ ] Explore trace-informed nudges for escaping, parameterization, and safe wrappers
- [ ] Explore persisted cross-session query history with ranking heuristics
- [ ] Explore richer graph-action launchers once basic hotkey navigation is proven
- [ ] Re-evaluate any deferred feature only after measuring the baseline picker latency

## Notes

- These items are intentionally deferred because they add complexity or carry a
  higher risk of creeping into the typing path.
