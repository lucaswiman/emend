[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "emend-core"
version = "0.5.4"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
